import { DesocupadoPipe } from './desocupado.pipe';

describe('DesocupadoPipe', () => {
  it('create an instance', () => {
    const pipe = new DesocupadoPipe();
    expect(pipe).toBeTruthy();
  });
});
